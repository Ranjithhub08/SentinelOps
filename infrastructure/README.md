# Infrastructure

This directory contains configuration files and manifests for deploying and managing the SentinelOps platform.

* **docker**: Contains configurations for local development containers using Docker and Docker Compose.
* **kubernetes**: Contains deployment manifests (YAML) for orchestrating microservices in a Kubernetes cluster.
* **terraform**: Contains Terraform configurations for provisioning and managing cloud infrastructure resources.
